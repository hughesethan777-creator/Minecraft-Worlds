anvil-parser2
nbtlib

# Bedrock generator. On some systems `amulet-mutf8` (a transitive dependency)
# fails to build with newer setuptools ("AttributeError: install_layout") --
# work around it with:
#   SETUPTOOLS_USE_DISTUTILS=stdlib pip install -r scripts/requirements.txt
amulet-core
